# Prevent direct access to documentation files
# Only allow access through the application

# Deny all direct access
<FilesMatch "\.(md|csv|json|sql|txt)$">
  Order Allow,Deny
  Deny from all
</FilesMatch>

# Also deny directory listing
Options -Indexes

# Return 403 Forbidden for direct requests
ErrorDocument 403 "Access Denied"
